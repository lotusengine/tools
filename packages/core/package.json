{
  "name": "@lotusengine/core",
  "version": "1.0.0",
  "description": "LotusEngine core utilities",
  "keywords": [
    "typescript",
    "lotusengine"
  ],
  "homepage": "https://github.com/lotusengine/lotusengine#readme",
  "bugs": "https://github.com/lotusengine/lotusengine/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lotusengine/lotusengine.git"
  },
  "license": "MIT",
  "author": "LotusEngine {https://lotusengine.com/)",
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "tsc --build tsconfig.build.json --clean",
    "wipe": "rimraf dist",
    "coverage": "jest --coverage",
    "depcheck": "dependency-check . --unused --no-dev && dependency-check . --missing --no-dev",
    "prepublishOnly": "yarn build && yarn bundle",
    "test": "jest",
    "verify": "yarn lint && yarn build && yarn depcheck && yarn coverage && yarn bundle",
    "watch": "jest --watch"
  },
  "dependencies": {
    "clean-deep": "^3.4.0",
    "deep-equal": "^2.0.5",
    "fs-extra": "^10.0.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@lotusengine/types": "file:../types",
    "@types/deep-equal": "^1.0.1",
    "@types/lodash": "^4.14.173"
  }
}
