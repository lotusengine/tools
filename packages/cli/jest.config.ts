import base from '../../jest.config.base'

export default {
  ...base,
  name: '@lotusengine/sdk',
  displayName: 'SDK',
  rootDir: '.'
  //testMatch: [`<rootDir>/packages/constructs/**/*.test.ts`]
}
